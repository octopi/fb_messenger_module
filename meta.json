/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [
        {
            "id": "user_id",
            "title":"User ID"
        },
        {
            "id": "text_message",
            "title": "Text Message"
        },
        {
            "id": "attachment",
            "title": "Attachment Message (BYO-JSON)",
            "description": "Build your JSON object in Dexter code view. Needs to conform to FB's specs"
        }
    ],
    //Required. Outputs the module emits.
    "outputs": [
    ],
    "title": "Send Facebook Message",
	"icon": "dex-messenger"
}
